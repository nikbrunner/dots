-- Prettier rendering of :help files (decorations, links, code blocks).
-- Self-activates on the help filetype; no setup needed.

Edit.later(function()
	vim.pack.add({ "git@github.com:OXY2DEV/helpview.nvim" })
end)
