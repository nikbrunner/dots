-- JSON/YAML schema catalog. No setup — required by the jsonls/yamlls
-- LSP configs via `require("schemastore")`.

Edit.later(function()
	vim.pack.add({ "git@github.com:b0o/SchemaStore.nvim" })
end)
