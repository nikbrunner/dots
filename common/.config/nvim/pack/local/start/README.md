# Local plugins

Plugins developed in this repository. Nvim loads every `pack/*/start`
directory automatically at startup — no `vim.pack` involvement, so edits
apply live and there is no lockfile entry.

This directory is the runtimepath counterpart of `plugin/50_specs/`:
specs describe external plugins, everything here is our own code.
