# TODO

- [~] Make Snippets like `today` work in `mini.files` buffers (daily notes)
- [x] Resolve `tab` conflict mapping (supermaven vs mdn.nvim)
- [x] `mini.files` should not open by default (maybe disable netwr completely)
- [x] `messages` window via `:h ui2`
- [x] Jumpable diagnostic window for easy copy or quickfixable command for
      mini.pick
